# To-do

List of pending tasks to do.
All of them should be done by the project's deadline.

## Mandatory Tasks 

### High

1. Add a Regex implementation. In which, for specific symbols, we use regex as to get a list of tokens used within the input we have. 
    - These _specific symbols_ haven't been decided _yet_, but the implementation should be so that we can swap symbols easily once they're determined. I.e. We can use placeholder symbols and replace them afterwards.
2. Add a proper transition table implementation. Should be done in `src/constants/transitionTable.ts`. It should be documented as well.

### Medium


### Low

1. Add more triton examples to `/input`.
